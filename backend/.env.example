# ==============================================
# Django Settings
# ==============================================
# Generate a new secret key for production: python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'
SECRET_KEY=django-insecure-o94umkme4o%sbc2uv3$7!hu^ycp(_o*h(3-&!vetn5h^2#oi_t

# Debug mode - MUST be False in production
DEBUG=True

# Django settings module to use
DJANGO_SETTINGS_MODULE=desideliver_backend.settings.local

# Allowed hosts (comma-separated, no spaces)
# Development: localhost,127.0.0.1,0.0.0.0
# Production: yourdomain.com,www.yourdomain.com
ALLOWED_HOSTS=localhost,127.0.0.1,0.0.0.0

# ==============================================
# Database Configuration (PostgreSQL)
# ==============================================
DB_ENGINE=django.db.backends.postgresql
DB_NAME=desideliver_dev
DB_USER=desideliver_user
DB_PASSWORD=dev_password_change_in_production
DB_HOST=db
DB_PORT=5432

# ==============================================
# Redis Configuration
# ==============================================
REDIS_URL=redis://redis:6379/0

# ==============================================
# Email Configuration
# ==============================================
# Options: django.core.mail.backends.console.EmailBackend (dev)
#          django.core.mail.backends.smtp.EmailBackend (production)
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend

# SMTP Settings (for production)
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=your-email@gmail.com
# EMAIL_HOST_PASSWORD=your-app-password
# DEFAULT_FROM_EMAIL=noreply@desideliver.com

# SendGrid Configuration (optional - alternative to SMTP)
SENDGRID_API_KEY=
DELIVERY_COORDINATOR_EMAIL=delivery@desideliver.com

# ==============================================
# CORS Configuration
# ==============================================
# Frontend URLs allowed to access the API (comma-separated, no spaces)
# Development: http://localhost:3000,http://localhost:3001
# Production: https://yourdomain.com,https://www.yourdomain.com
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001

# ==============================================
# Security Settings (Production Only)
# ==============================================
# Uncomment and set to True in production
# SECURE_SSL_REDIRECT=True
# SESSION_COOKIE_SECURE=True
# CSRF_COOKIE_SECURE=True
# SECURE_HSTS_SECONDS=31536000
# SECURE_HSTS_INCLUDE_SUBDOMAINS=True
# SECURE_HSTS_PRELOAD=True

# ==============================================
# Application Settings
# ==============================================
# Custom application settings
# Add any custom environment variables your app needs below
